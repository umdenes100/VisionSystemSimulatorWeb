#include "State.h"

void State::init(char *json)
{
    // do what we want
}

void State::update(VisionSystemClient Enes100, Tank tank, SerialClient Serial)
{
    // do what we want
}

extern State lib_state;

void delay(int msec) 
{
    lib_state.init("");
}